version: '3'

services:
  angular:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - configuration=${CONFIGURATION}
    container_name: matrimonio_${CONFIGURATION}
    expose:
      - 5001
    image: dz/matrimonio_${CONFIGURATION}
    restart: unless-stopped
